export interface Dish {
    name: string;
    dishType: string,
    description: string,
    recipe: string,
    preparationPrice: number,
    purchasePrice: number,
    discount: number,
    preparationTime: number,
    ingredients: string[]
}