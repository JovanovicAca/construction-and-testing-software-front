export interface Drink {
    name: string;
    drinkType: string,
    description: string,
    recipe: string,
    preparationPrice: number,
    purchasePrice: number,
    discount: number,
    ingredients: string[]
}